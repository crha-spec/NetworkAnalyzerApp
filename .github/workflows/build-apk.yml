name: Build Android APK

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'
        cache: 'npm'

    - name: Setup JDK 11
      uses: actions/setup-java@v3
      with:
        distribution: 'zulu'
        java-version: '11'

    - name: Cache node modules
      uses: actions/cache@v3
      with:
        path: node_modules
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-

    - name: Cache Gradle
      uses: actions/cache@v3
      with:
        path: |
          ~/.gradle/caches
          ~/.gradle/wrapper
        key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
        restore-keys: |
          ${{ runner.os }}-gradle-

    - name: Install dependencies
      run: npm install

    - name: Grant execute permission for gradlew
      run: chmod +x android/gradlew

    - name: Clean build
      run: |
        cd android
        ./gradlew clean

    - name: Build APK
      run: |
        cd android
        ./gradlew assembleRelease --stacktrace

    - name: Upload APK as artifact
      uses: actions/upload-artifact@v3
      with:
        name: app-release-apk
        path: android/app/build/outputs/apk/release/app-release.apk
        retention-days: 30

    - name: Get current date
      id: date
      run: echo "date=$(date +'%Y-%m-%d-%H-%M')" >> $GITHUB_OUTPUT

    - name: Create Release
      if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master')
      uses: softprops/action-gh-release@v1
      with:
        tag_name: v1.0.${{ github.run_number }}
        name: Release v1.0.${{ github.run_number }} (${{ steps.date.outputs.date }})
        body: |
          ## 🚀 Network Analyzer Pro - Android APK
          
          ### 📦 Build Details
          - **Version:** v1.0.${{ github.run_number }}
          - **Build Date:** ${{ steps.date.outputs.date }}
          - **Commit:** ${{ github.sha }}
          
          ### 📥 İndirme
          Aşağıdaki APK dosyasını indirin ve Android cihazınıza yükleyin.
          
          ### ⚠️ Kurulum
          1. APK'yı indirin
          2. Ayarlar → Güvenlik → Bilinmeyen Kaynaklardan Yükle (izin verin)
          3. APK'yı açın ve yükleyin
          
          ### ✨ Özellikler
          - Network trafiği yakalama
          - HTTP/HTTPS analizi
          - Oyun trafiği tespiti
          - Gerçek zamanlı izleme
          
        files: android/app/build/outputs/apk/release/app-release.apk
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}